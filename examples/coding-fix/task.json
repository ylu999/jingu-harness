{
  "id": "fix-sum-fn",
  "goal": "Fix the sum function in src/math.js so it returns a + b (currently returns a - b)",
  "allowedFiles": ["src/**"],
  "maxRetries": 3,
  "verify": {
    "type": "command",
    "command": "node --test tests/task.test.js",
    "pass": { "exitCode": 0 }
  }
}
